{
  "name": "book-printing-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build && cd ../backend && npm install && npm run build",
    "start": "cd backend && node dist/server.js",
    "migrate": "cd backend && npm run migrate",
    "seed": "cd backend && npm run seed"
  },
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  }
}
